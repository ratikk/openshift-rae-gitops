apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount | default "hello-openshift" }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- with .Values.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
