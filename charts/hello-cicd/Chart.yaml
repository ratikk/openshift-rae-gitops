apiVersion: v2
name: hello-cicd
description: Minimal app to demo multi-environment GitOps (idev1/2/3 on parallel releases)
type: application
version: 0.1.0          # chart version — bump when TEMPLATES change, not on release promotion
appVersion: "1.0"      # informational; the real release is values.image.tag
